from Tools import *

# Average Spectrums
links = getLinks()

# Spectrums, Labels = getData(links)
#
# Spectrums = averagePiecewise(Spectrums, Labels)
# plot(Spectrums, Labels, 'All Maltol', 1)
#
# plot(Spectrums, Spectrums.columns[[1, 11]], 'Average Spectrums', 2)
